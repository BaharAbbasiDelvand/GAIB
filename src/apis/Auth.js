// require('dotenv').config();
// const url = process.env.AUTH_URL;
// import path from 'path';
// import dotenv from 'dotenv';

export const sendRegister = (props) => {
    const { userData } = props;
    console.log(userData);
    // console.log(url)
};
